[project]
name = "pratapan-sidecar"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pyspark>=4.0.0",
    "pyyaml>=6.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.0",
]
